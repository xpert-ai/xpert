/**
 * @description
 * Current version of the XpertAI API.
 *
 * @example
 * ```
 * import { version } from './version';
 *
 * console.log('XpertAI API version:', version);
 * ```
 *
 * @since 0.1.0
 */
export const version: string = require('./package.json').version;
