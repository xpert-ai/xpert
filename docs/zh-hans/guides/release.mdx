# 发布 npm 包

## 升级版本号

```bash
pnpm nx release patch
```

## 构建包

```bash
pnpm nx build plugin-sdk
```

## 发布包

```bash
pnpm nx run plugin-sdk:nx-release-publish --access public --otp=<one-time-password-if-needed>
```
