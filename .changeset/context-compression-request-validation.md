---
'@xpert-ai/plugin-sdk': patch
'@xpert-ai/contracts': patch
---

Add optional middleware hooks for final model-request validation and prepared Agent state updates, including typed access to the active model configuration and event subscriber. Extend shared contracts with context-compression failure reasons, model context-window provenance, and active execution window metadata.
