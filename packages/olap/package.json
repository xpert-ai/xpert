{
  "name": "@xpert-ai/ocap-olap",
  "version": "1.1.0",
  "private": true,
  "license": "AGPL-3.0",
  "scripts": {
    "build": "rimraf target && mvn install && pnpm run docker-build",
    "mvn": "mvn install",
    "docker-build": "docker build --tag xpert/olap .",
    "start": "sh ./run.sh",
    "debug": "sh ./debug.sh"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
