# draft

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build draft` to build the library.

## Running unit tests

Run `nx test draft` to execute the unit tests via [Jest](https://jestjs.io).
