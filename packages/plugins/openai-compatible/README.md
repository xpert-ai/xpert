# openai-compatible

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build openai-compatible` to build the library.

## Running unit tests

Run `nx test openai-compatible` to execute the unit tests via [Jest](https://jestjs.io).
