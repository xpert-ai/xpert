/**
 * Raw provider-neutral Browser Runtime Definition.
 * Core intentionally validates this JSON at startup before treating it as a typed Definition.
 */
declare const browserRuntimeDefinition: unknown

export default browserRuntimeDefinition
