Example skill bundles live here for reference only.

They are intentionally nested under `examples/` so bootstrap will not load them.

To activate one, copy the example directory to the first level under `skill-packages/`, for example:

```text
skill-packages/claude-api-bundle/
  bundle.yaml
  SKILL.md
```
